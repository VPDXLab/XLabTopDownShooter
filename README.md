# XLab Top-Down Shooter

Unity проект с изометрической камерой и системой управления персонажем в стиле MOBA/RTS.

## 🎮 Описание

Проект реализует базовую механику управления персонажем в top-down игре:
- Перемещение по клику правой кнопки мыши
- Поворот персонажа в направлении курсора
- Визуальный маркер цели движения
- Плавное следование камеры за персонажем

## 🏗️ Архитектура проекта

### Структура папок

```
Assets/
├── Scripts/           # Исходный код игры
│   ├── Players/      # Логика игрока
│   ├── Cameras/      # Система камеры
│   └── Markers/      # Визуальные маркеры
├── Data/             # ScriptableObjects с конфигурацией
├── Prefabs/          # Префабы игровых объектов
├── _Scenes/          # Игровые сцены
├── Art/              # Графические ресурсы
└── Settings/         # Настройки проекта
```

## 📦 Компоненты системы

### 1. Player System (`Assets/Scripts/Players/`)

#### PlayerController
**Назначение:** Главный контроллер игрока, координирующий все подсистемы.

**Основные функции:**
- Обработка ввода мыши
- Управление поворотом персонажа к курсору
- Установка целевой точки движения по клику ПКМ

**Зависимости:**
- `PlayerMovement` - компонент движения
- `NavMeshMouseResolver` - разрешение точек на NavMesh
- `PlayerRotationCalculator` - расчет направления взгляда
- `PlayerConfig` - конфигурация параметров

#### PlayerMovement
**Назначение:** Управление движением персонажа через NavMesh.

**Основные функции:**
- Движение к целевой точке через `NavMeshAgent`
- Ручной поворот персонажа (отключена автоматическая ротация NavMesh)
- События: `Stopped`, `DestinationChanged`

**Ключевые методы:**
- `SetDestination(Vector3)` - установить целевую точку
- `RotateTowards(Vector3)` - повернуть персонажа к точке
- `Initialize(float speed, float angularSpeed)` - инициализация параметров

#### PlayerRotationCalculator
**Назначение:** Расчет точки в мире для поворота персонажа к курсору.

**Принцип работы:**
1. Конвертирует позицию игрока в экранные координаты
2. Вычисляет дельту между курсором и игроком
3. Преобразует экранную дельту в мировое направление с учетом поворота камеры
4. Возвращает точку в мире для направления взгляда

#### NavMeshMouseResolver
**Назначение:** Определение валидных точек на NavMesh по позиции мыши.

**Основные функции:**
- Raycast от камеры в мир по позиции курсора
- Поиск ближайшей точки на NavMesh
- Фильтрация по LayerMask

**Параметры:**
- `m_layerMask` - маска слоев для raycast
- `m_raycastDistance` - максимальная дистанция луча
- `m_navMeshSampleMaxDistance` - радиус поиска точки на NavMesh

#### PlayerConfig (ScriptableObject)
**Назначение:** Хранение настроек игрока.

**Параметры:**
- `speed` - скорость движения (0-100)
- `angularSpeed` - скорость поворота (градусы/сек)

**Расположение:** `Assets/Data/PlayerConfig.asset`

### 2. Camera System (`Assets/Scripts/Cameras/`)

#### CameraFollow
**Назначение:** Плавное следование камеры за целью.

**Основные функции:**
- Следование за `Transform` цели с заданным смещением
- Плавное сглаживание движения через `Vector3.SmoothDamp`

**Параметры:**
- `m_target` - цель для слежения
- `m_offset` - смещение от цели (по умолчанию: Y=15, Z=-10)
- `m_smoothTime` - время сглаживания (0.001-1.0)

### 3. Marker System (`Assets/Scripts/Markers/`)

#### TargetMarker
**Назначение:** Визуальный индикатор целевой точки движения.

**Основные функции:**
- Показ маркера в указанной точке
- Анимация пульсации через DOTween
- Автоматическое скрытие

**Параметры:**
- `m_startSize` - начальный размер
- `m_finishSize` - конечный размер
- `m_duration` - длительность анимации
- `m_ease` - тип интерполяции (по умолчанию: InOutSine)

**Зависимости:** DOTween

#### TargetMarkerObserver
**Назначение:** Связывание маркера с событиями движения игрока.

**Принцип работы:**
- Подписывается на события `PlayerMovement`
- При смене цели (`DestinationChanged`) - показывает маркер
- При остановке (`Stopped`) - скрывает маркер

## 🔧 Технические детали

### Input System
Используется новая Input System от Unity:
- `UnityEngine.InputSystem`
- Чтение позиции мыши: `Mouse.current.position.ReadValue()`
- Проверка нажатия ПКМ: `Mouse.current.rightButton.wasPressedThisFrame`

### Navigation
- Unity NavMesh для pathfinding
- Отключена автоматическая ротация агента (`updateRotation = false`)
- Ручной контроль поворота через `Quaternion.RotateTowards`

### Camera
- Изометрическая проекция (параметры камеры настраиваются вручную)
- Использование мировых осей камеры для расчета направлений

### Animation
- DOTween для анимации маркера
- Бесконечный Yoyo loop для эффекта пульсации

## 🎯 Паттерны проектирования

1. **Event-Driven Architecture**
   - `PlayerMovement` использует события для уведомления об изменениях
   - `TargetMarkerObserver` реагирует на события через подписку

2. **ScriptableObject Configuration**
   - `PlayerConfig` для хранения настроек вне кода
   - Переиспользуемость конфигураций

3. **Single Responsibility**
   - Каждый класс отвечает за одну задачу
   - `PlayerRotationCalculator` - отдельный класс для расчетов

4. **Composition over Inheritance**
   - `PlayerController` композирует другие компоненты
   - Гибкая архитектура без глубоких иерархий

## 🚀 Использование

### Настройка игрока

1. Создайте GameObject с компонентом `PlayerController`
2. Добавьте `NavMeshAgent` (автоматически через RequireComponent)
3. Назначьте или создайте `PlayerConfig` в папке Data
4. Убедитесь, что на сцене есть NavMesh

### Настройка камеры

1. Добавьте `CameraFollow` на Main Camera
2. Установите ссылку на Transform игрока
3. Настройте offset и smoothTime по необходимости

### Настройка маркера

1. Создайте GameObject для маркера с визуальным представлением
2. Добавьте компонент `TargetMarker`
3. Создайте GameObject с `TargetMarkerObserver`
4. Свяжите маркер и `PlayerMovement`

## 📝 Зависимости

- **Unity Version:** 2021.3+ (рекомендуется)
- **Packages:**
  - Unity Input System
  - Unity AI Navigation (NavMesh)
  - DOTween (для анимации маркера)

## 🔮 Возможности для расширения

- Добавление системы боя
- Инвентарь и предметы
- Система способностей
- Множественные персонажи
- Система противников
- Сохранение/загрузка
- UI система
